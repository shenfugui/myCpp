#include <bits/stdc++.h>

using namespace std;

const int N = 510;

int p, f, c;
int p[N];

int main() {
    cin >> p >> f >> c;
    for (int i = 0; i < p; i++) {
        int a, b, t;
        scanf("%d%d%d", &a, &b, &t);
    }
}